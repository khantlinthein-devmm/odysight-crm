---
import DashboardLayout from "../../layouts/DashboardLayout.astro";
import CleanerDetail from "../../components/vue/cleaners/CleanerDetail.vue";

export const prerender = false;

const { id } = Astro.params;
---

<DashboardLayout title={`Cleaner #${id}`}>
  <CleanerDetail client:load cleanerId={Number(id)} />
</DashboardLayout>